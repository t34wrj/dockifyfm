version: '3.8'

services:
  dockifyfm:
    build: .
    volumes:
      - ./.env:/app/.env
    environment:
      - TZ=UTC
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-m", "dockifyfm", "health"]
      interval: 30s
      timeout: 10s
      retries: 3 